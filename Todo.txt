todo: 
    1. Scheduler turn into a CMD application with goflags 
        - type of fetcher (playwrightfetcher or http) set through CMD flags
        - allow specifing the .env file location through flags 
            - .env will be the enviorment the fetcher will save to
        - see /plans/SCHEDULER_CLI_PLAN.md
        - see /plans/plawright-config-plan.md
    1. Playwright fetcher  
        - Investigate how to return network data 
        - add notes on play wright bot detection as readme

    2. Cralwer Design
        2. Update crawler to get more speicified data or design a data splitter / organiser to validate the data before feeding it to the LLM for job Parsing

            Use Ollama in docker and parse html with the api.
                - load ollama in docker
                - retrieve data from database 
                - call ollama to parse the data and return json format 

                - Raw HTML is large - crawler needs to go to individual pages and store that information via the browserfetcher 
                    - each post is small enough to pass to the LLM to extract data 

            LLM Parser 
                - Expected data limit of 16GB (Hardware limitation) 
                - Sanitise the input ( trim white space, remove unwanted html elements like script etc)
                - Parser needs to work with the hardware limitations
                    Strategy:
                        1. Parse the HTML via goquery to find each jobs root element then use the LLM to extract the job data
                        2. Cralwer goes to each job page rather than supply the main board 
                            1. navigate all the links with in the domain by finding the job links (go query or llm strategy)

            Crawler
                1. Fetcher issue, The current HTTPFetcher uses a simple HTTP GET request which only retrieves the initial HTML. For Single Page Applications (SPAs), JavaScript needs to execute to render the full content. We need to use a headless browser approach.
                    Solution
                    Create a new BrowserFetcher that uses the chromedp package to launch a headless Chrome browser, wait for the page to fully load (including JS execution), then read the rendered HTML body.
                    Keep the existing HTTPFetcher for non-SPA pages.
                    https://github.com/chromedp/chromedp

                    1. mimic searching the site as a user programatically

    2. Crawler concurrency deadlock 
        One thing to be aware of: dropping links means some pages won't be crawled when under heavy load. 
        If completeness matters, 
        an alternative would be to use a separate dispatcher goroutine that buffers jobs in an unbounded in-memory queue, 
        but for most crawlers the drop approach is acceptable.
    
    3. Testing
        - LLM testing can't test the output of llm through unit tests have to do structure so that llm service can be mocked
        - golangwebcrawler/internal/dbstore       3.348s  coverage: 64.5% of statements

    4. Parsing Scheduler ( does it need an api to kick off maybe this should be in the api and parser to has and api to run types of scheudling like jobs )

    Issue : Can't scrape real sites because of terms of usage etc.
        practise on fake sites first https://realpython.github.io/fake-jobs/
        https://www.scrapingbee.com/blog/scraper-sites/

        For Practice: Stick to Specialized models. It teaches you how to inspect the DOM, find unique selectors, and handle specific page layouts.
        For a Product: Start with a General parser that looks for application/ld+json first. It’s the "cheat code" of web scraping—it gives you clean, structured data (Title, Description, DatePosted) without you having to write site-specific code.

    5. update go version
